# 配置完成清单

## ✅ 已完成的配置

### 1. 环境变量配置
- ✅ 创建了 `/Users/gary/Desktop/申报/backend/.env`
- ✅ 填写了豆包 API 配置：
  - API Key: bd54d197-96c0-47e8-b5cc-baa8d4cb5e1a
  - Base URL: https://ark.cn-beijing.volces.com/api/v3
  - Model: doubao-seed-2-0-pro-260215
- ✅ 填写了爬虫账号：
  - 用户名: 13825269170
  - 密码: 201402

### 2. 浏览器安装
- ✅ 安装了 playwright
- ✅ 安装了 Chromium 浏览器

### 3. 目录创建
- ✅ 创建了 uploads 目录（上传文件）
- ✅ 创建了 outputs 目录（输出文件）

### 4. 启动脚本
- ✅ 创建了 `start-dev.sh` 快速启动脚本

---

## 🚀 现在可以启动了！

### 方式1：使用启动脚本（推荐）

```bash
cd /Users/gary/Desktop/申报
./start-dev.sh
```

这个脚本会自动：
1. 检查依赖
2. 启动后端（http://localhost:8000）
3. 启动前端（http://localhost:3000）
4. 显示访问地址

### 方式2：手动启动

**终端1 - 启动后端：**
```bash
cd /Users/gary/Desktop/申报/backend
python3 -m uvicorn app.main:app --reload --port 8000
```

**终端2 - 启动前端：**
```bash
cd /Users/gary/Desktop/申报/frontend
npm install  # 首次需要
npm run dev
```

---

## 📍 访问地址

- **前端界面**: http://localhost:3000
- **后端API**: http://localhost:8000
- **API文档**: http://localhost:8000/docs （Swagger UI）

---

## 🧪 测试步骤

1. 打开浏览器访问 http://localhost:3000
2. 点击"设置"，检查 LLM 配置是否正确
3. 点击"测试连接"，验证豆包 API
4. 回到首页，上传测试文件
5. 点击"开始处理"
6. 查看处理进度和结果

---

## 📁 配置文件位置

- **后端配置**: `/Users/gary/Desktop/申报/backend/.env`
- **前端配置**: `/Users/gary/Desktop/申报/frontend/.env`

如需修改配置，编辑这些文件后重启服务即可。

---

## ⚠️ 注意事项

1. **首次启动**可能需要安装依赖，会比较慢
2. **端口占用**：确保 3000 和 8000 端口没有被占用
3. **Python 版本**：需要 Python 3.10+
4. **Node 版本**：需要 Node.js 18+

---

## 🐛 如果遇到问题

### 后端启动失败
```bash
cd backend
pip3 install -r requirements.txt
python3 -m uvicorn app.main:app --reload
```

### 前端启动失败
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### 豆包 API 连接失败
检查 `.env` 文件中的 API Key 是否正确

---

准备好了！现在可以运行 `./start-dev.sh` 启动系统了！
