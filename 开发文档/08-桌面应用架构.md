# 桌面应用架构设计

## 1. 技术选型

### 1.1 桌面应用框架

**选择：Electron**

理由：
- ✅ 跨平台（Windows/Mac/Linux）
- ✅ 可以复用 Web 前端代码
- ✅ 打包成 MSI 简单
- ✅ 生态成熟

### 1.2 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                  Electron 主进程                         │
│  - 窗口管理                                              │
│  - 文件系统访问                                          │
│  - 本地数据库（SQLite）                                  │
│  - 后台任务调度                                          │
└────────────────────┬────────────────────────────────────┘
                     │ IPC 通信
┌────────────────────▼────────────────────────────────────┐
│                  Electron 渲染进程                       │
│  - React 前端界面                                        │
│  - 用户交互                                              │
└────────────────────┬────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────┐
│                  Python 后端服务                         │
│  - 文档解析                                              │
│  - 税率查询（爬虫）                                      │
│  - HS编码优化                                            │
│  - 文件生成                                              │
└─────────────────────────────────────────────────────────┘
```

### 1.3 部署方式

**方案：内嵌 Python 运行时**

```
安装包结构：
customs-optimizer.msi
├── electron/              # Electron 应用
├── python/                # Python 运行时（内嵌）
│   ├── python.exe
│   ├── Lib/
│   └── Scripts/
├── backend/               # Python 后端代码
│   ├── app/
│   └── requirements.txt
└── data/                  # 数据目录
    ├── database.db        # SQLite 数据库
    ├── uploads/           # 上传文件
    └── outputs/           # 输出文件
```

---

## 2. 技术栈

### 2.1 前端（Electron 渲染进程）

```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "antd": "^5.13.0",
    "zustand": "^4.5.0",
    "axios": "^1.6.5",
    "react-dropzone": "^14.2.3"
  }
}
```

### 2.2 Electron 主进程

```json
{
  "dependencies": {
    "electron": "^28.0.0",
    "electron-store": "^8.1.0",
    "better-sqlite3": "^9.2.0"
  },
  "devDependencies": {
    "electron-builder": "^24.9.0"
  }
}
```

### 2.3 Python 后端

```python
# requirements.txt
fastapi==0.109.0
uvicorn==0.27.0
pandas==2.2.0
openpyxl==3.1.2
playwright==1.41.0
requests==2.31.0
jieba==0.42.1
```

---

## 3. 项目结构

```
customs-optimizer/
├── electron/                    # Electron 主进程
│   ├── main.js                  # 主进程入口
│   ├── preload.js               # 预加载脚本
│   └── python-bridge.js         # Python 桥接
├── frontend/                    # React 前端
│   ├── src/
│   ├── package.json
│   └── vite.config.ts
├── backend/                     # Python 后端
│   ├── app/
│   │   ├── main.py
│   │   ├── services/
│   │   └── utils/
│   ├── requirements.txt
│   └── build.py                 # 打包脚本
├── build/                       # 构建配置
│   ├── electron-builder.yml     # Electron Builder 配置
│   └── installer.nsi            # NSIS 安装脚本
├── resources/                   # 资源文件
│   ├── icon.ico
│   └── python-runtime/          # Python 运行时
├── package.json                 # 根 package.json
└── README.md
```

---

## 4. 核心实现

### 4.1 Electron 主进程

```javascript
// electron/main.js

const { app, BrowserWindow, ipcMain } = require('electron');
const path = require('path');
const { spawn } = require('child_process');
const Store = require('electron-store');

const store = new Store();
let mainWindow;
let pythonProcess;

// 创建窗口
function createWindow() {
  mainWindow = new BrowserWindow({
    width: 1200,
    height: 800,
    webPreferences: {
      preload: path.join(__dirname, 'preload.js'),
      nodeIntegration: false,
      contextIsolation: true
    }
  });

  // 加载前端
  if (process.env.NODE_ENV === 'development') {
    mainWindow.loadURL('http://localhost:3000');
  } else {
    mainWindow.loadFile(path.join(__dirname, '../frontend/dist/index.html'));
  }
}

// 启动 Python 后端
function startPythonBackend() {
  const pythonPath = path.join(
    process.resourcesPath,
    'python',
    'python.exe'
  );

  const backendPath = path.join(
    process.resourcesPath,
    'backend',
    'app',
    'main.py'
  );

  pythonProcess = spawn(pythonPath, [
    '-m', 'uvicorn',
    'app.main:app',
    '--host', '127.0.0.1',
    '--port', '8000'
  ], {
    cwd: path.join(process.resourcesPath, 'backend')
  });

  pythonProcess.stdout.on('data', (data) => {
    console.log(`Python: ${data}`);
  });

  pythonProcess.stderr.on('data', (data) => {
    console.error(`Python Error: ${data}`);
  });
}

// 应用启动
app.whenReady().then(() => {
  startPythonBackend();
  createWindow();
});

// 应用退出
app.on('quit', () => {
  if (pythonProcess) {
    pythonProcess.kill();
  }
});

// IPC 通信
ipcMain.handle('get-settings', () => {
  return store.get('settings', {});
});

ipcMain.handle('save-settings', (event, settings) => {
  store.set('settings', settings);
  return { success: true };
});

ipcMain.handle('select-files', async () => {
  const { dialog } = require('electron');
  const result = await dialog.showOpenDialog(mainWindow, {
    properties: ['openFile', 'multiSelections'],
    filters: [
      { name: 'Excel Files', extensions: ['xlsx', 'xls'] }
    ]
  });
  return result.filePaths;
});
```

### 4.2 预加载脚本

```javascript
// electron/preload.js

const { contextBridge, ipcRenderer } = require('electron');

// 暴露安全的 API 给渲染进程
contextBridge.exposeInMainWorld('electronAPI', {
  // 设置相关
  getSettings: () => ipcRenderer.invoke('get-settings'),
  saveSettings: (settings) => ipcRenderer.invoke('save-settings', settings),

  // 文件选择
  selectFiles: () => ipcRenderer.invoke('select-files'),

  // 打开外部链接
  openExternal: (url) => ipcRenderer.invoke('open-external', url)
});
```

### 4.3 前端调用

```typescript
// frontend/src/services/electron.ts

// 类型定义
interface ElectronAPI {
  getSettings: () => Promise<any>;
  saveSettings: (settings: any) => Promise<any>;
  selectFiles: () => Promise<string[]>;
  openExternal: (url: string) => Promise<void>;
}

declare global {
  interface Window {
    electronAPI: ElectronAPI;
  }
}

// 检测是否在 Electron 环境
export const isElectron = () => {
  return window.electronAPI !== undefined;
};

// 获取设置
export const getSettings = async () => {
  if (isElectron()) {
    return await window.electronAPI.getSettings();
  }
  // Web 环境降级
  return JSON.parse(localStorage.getItem('settings') || '{}');
};

// 保存设置
export const saveSettings = async (settings: any) => {
  if (isElectron()) {
    return await window.electronAPI.saveSettings(settings);
  }
  // Web 环境降级
  localStorage.setItem('settings', JSON.stringify(settings));
};

// 选择文件
export const selectFiles = async () => {
  if (isElectron()) {
    return await window.electronAPI.selectFiles();
  }
  // Web 环境降级：使用 input file
  return new Promise<File[]>((resolve) => {
    const input = document.createElement('input');
    input.type = 'file';
    input.multiple = true;
    input.accept = '.xlsx,.xls';
    input.onchange = (e) => {
      const files = Array.from((e.target as HTMLInputElement).files || []);
      resolve(files);
    };
    input.click();
  });
};
```

### 4.4 文件上传组件（适配 Electron）

```typescript
// frontend/src/components/FileUpload.tsx

import React from 'react';
import { Button, Upload } from 'antd';
import { isElectron, selectFiles } from '../services/electron';

export const FileUpload: React.FC = () => {
  const [files, setFiles] = React.useState<string[]>([]);

  const handleSelectFiles = async () => {
    if (isElectron()) {
      // Electron 环境：使用原生文件选择器
      const filePaths = await selectFiles();
      setFiles(filePaths);
    } else {
      // Web 环境：使用 HTML input
      // ... 原有逻辑
    }
  };

  return (
    <div>
      <Button onClick={handleSelectFiles}>选择文件</Button>

      {files.map((file, index) => (
        <div key={index}>{file}</div>
      ))}
    </div>
  );
};
```

---

## 5. 打包配置

### 5.1 Electron Builder 配置

```yaml
# build/electron-builder.yml

appId: com.customs.optimizer
productName: 清关优化系统
copyright: Copyright © 2026

directories:
  output: dist
  buildResources: resources

files:
  - electron/**/*
  - frontend/dist/**/*
  - package.json

extraResources:
  - from: backend
    to: backend
  - from: resources/python-runtime
    to: python

win:
  target:
    - target: nsis
      arch:
        - x64
  icon: resources/icon.ico

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: 清关优化系统
  installerIcon: resources/icon.ico
  uninstallerIcon: resources/icon.ico
  installerHeaderIcon: resources/icon.ico
  deleteAppDataOnUninstall: true

# MSI 配置
msi:
  oneClick: false
  perMachine: true
```

### 5.2 打包脚本

```json
// package.json

{
  "name": "customs-optimizer",
  "version": "1.0.0",
  "main": "electron/main.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:electron\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && python -m uvicorn app.main:app --reload",
    "dev:electron": "electron .",

    "build": "npm run build:frontend && npm run build:backend && npm run build:electron",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "python backend/build.py",
    "build:electron": "electron-builder --win --x64",

    "pack": "electron-builder --dir",
    "dist": "electron-builder"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "electron": "^28.0.0",
    "electron-builder": "^24.9.0"
  }
}
```

### 5.3 Python 打包脚本

```python
# backend/build.py

import os
import shutil
import subprocess
from pathlib import Path

def build_python_runtime():
    """打包 Python 运行时"""
    print("正在打包 Python 运行时...")

    # 使用 PyInstaller 或直接复制 Python 运行时
    # 方案1: 使用 embeddable Python
    # 下载 Python embeddable zip
    # 解压到 resources/python-runtime/

    # 方案2: 使用 PyInstaller
    subprocess.run([
        'pyinstaller',
        '--onedir',
        '--name', 'backend',
        'app/main.py'
    ])

    print("Python 运行时打包完成")

def install_dependencies():
    """安装依赖到运行时"""
    print("正在安装依赖...")

    subprocess.run([
        'pip', 'install',
        '-r', 'requirements.txt',
        '-t', '../resources/python-runtime/Lib/site-packages'
    ])

    print("依赖安装完成")

if __name__ == '__main__':
    build_python_runtime()
    install_dependencies()
```

---

## 6. 开发流程

### 6.1 开发环境

```bash
# 1. 安装依赖
npm install
cd frontend && npm install
cd ../backend && pip install -r requirements.txt

# 2. 启动开发环境
npm run dev

# 这会同时启动：
# - 前端开发服务器 (http://localhost:3000)
# - Python 后端 (http://localhost:8000)
# - Electron 窗口
```

### 6.2 构建流程

```bash
# 1. 构建前端
cd frontend
npm run build

# 2. 打包 Python 后端
cd ../backend
python build.py

# 3. 构建 Electron 应用
cd ..
npm run build:electron

# 4. 生成 MSI 安装包
npm run dist

# 输出：dist/清关优化系统 Setup 1.0.0.exe
```

---

## 7. 优势

### 7.1 vs 纯 Web 应用

| 特性 | 桌面应用 | Web 应用 |
|------|---------|---------|
| 安装 | 一键安装 | 需要部署服务器 |
| 数据 | 本地存储 | 需要数据库服务器 |
| 性能 | 更快 | 依赖网络 |
| 离线 | 完全离线 | 需要联网 |
| 更新 | 自动更新 | 需要重新部署 |

### 7.2 用户体验

- ✅ 双击图标即可使用
- ✅ 数据存储在本地，更安全
- ✅ 不需要配置服务器
- ✅ 不需要联网（除了查询税率）
- ✅ 原生文件选择器
- ✅ 系统托盘集成

---

## 8. 部署方案

### 8.1 给客户

```
1. 发送 MSI 安装包
2. 客户双击安装
3. 安装完成后，桌面会有快捷方式
4. 双击快捷方式启动应用
```

### 8.2 给你测试（Web 版本）

```bash
# 启动 Web 版本
cd frontend
npm run dev

# 启动后端
cd backend
python -m uvicorn app.main:app --reload

# 访问 http://localhost:3000
```

---

## 9. 更新机制

### 9.1 自动更新

```javascript
// electron/main.js

const { autoUpdater } = require('electron-updater');

autoUpdater.checkForUpdatesAndNotify();

autoUpdater.on('update-available', () => {
  mainWindow.webContents.send('update-available');
});

autoUpdater.on('update-downloaded', () => {
  mainWindow.webContents.send('update-downloaded');
});
```

### 9.2 手动更新

在设置页面添加"检查更新"按钮。

---

## 10. 下一步

1. **搭建 Electron 项目**
2. **集成 Python 后端**
3. **适配前端代码**
4. **测试打包流程**
5. **生成 MSI 安装包**

需要我帮你搭建 Electron 项目吗？
