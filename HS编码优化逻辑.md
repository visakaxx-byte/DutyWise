# HS编码优化逻辑说明

## 整体流程

源码在 `backend/app/services/optimizer.py`。`HSCodeOptimizer` 对每个商品执行以下四步：

```
输入: 商品(品名 + HS编码) + 税率数据库(tax_data)
  │
  ├── ① 查找候选编码 _find_candidates()
  ├── ② 计算相似度     _calculate_similarity()
  ├── ③ 选择最优       _select_best_candidate()
  │
输出: 优化后的HS编码 或 保持原样
```

## 第1步：查找候选编码 `_find_candidates()`

- **前缀筛选**: 只考虑与原始编码**前4位相同**的HS编码（例如原始`8481901000` → 只搜`8481`开头的）
- **排除条件**:
  - 反倾销标记 + 用户开启`exclude_anti_dumping` → 跳过
  - `certification_required` → 跳过（需要资质认证的编码不用）
- **相似度门槛**: `similarity >= min_similarity (默认0.15)` 才纳入候选

## 第2步：品名匹配度 `_calculate_similarity()`

品名匹配度通过**混合 Jaccard 算法**计算，范围 0.0~1.0：

```
预处理: 去掉空格、/、- 符号

① Bigram 相似度 (权重50%)
  - "阀门垫圈" → bigram集合: {"阀门", "门垫", "垫圈"}
  - "阀门配件" → bigram集合: {"阀门", "门配", "配件"}
  - Jaccard = |交集 {"阀门"}| / |并集 {"阀门","门垫","垫圈","门配","配件"}| = 1/5 = 0.2

② 单字重叠 (权重50%)
  - 字集合交集: {"阀","门"} / 并集: {"阀","门","垫","圈","配","件"} = 2/6 ≈ 0.33

最终匹配度 = 0.2 × 0.5 + 0.33 × 0.5 = 0.267
```

## 第3步：选择最优 `_select_best_candidate()`

**综合得分公式**:

```
final_score = 税率得分 × 60% + 相似度得分 × 40%
```

**税率得分计算**:

```
新税率 < 原税率时: tax_score = 1.0 - (新税率/100)
  例: Free(0%) → 1.0,   5% → 0.95,   25% → 0.75
新税率 >= 原税率: tax_score = 0.0（直接淘汰）
```

**选择阈值**: `final_score > 0.55` 才采纳优化。设计理由：最优情况下（0%税率 + 最低相似度0.15）→ `1.0×0.6 + 0.15×0.4 = 0.66 > 0.55`，能通过。

## "品名匹配度 15%" 是怎么来的？

**15% 不是算出来的，而是一个保底下限。**

关键代码在 `optimizer.py`:

```python
similarity = max(0.15, self._calculate_similarity(original_name, info.get("description_cn", "")))
```

`_calculate_similarity()` 真实返回的可能只有 0.05、0.08 等低分（品名差异大时），但 `max(0.15, ...)` 强制兜底到 0.15。目的是：

1. **留一扇门**：即使品名文字不匹配（比如英文品名 vs 中文描述），只要 HS 前4位相同、税率更低，仍有资格进入候选
2. **让税率说话**：`.15×0.4 = 0.06` 对最终得分贡献很小，真正决定优化的是税率得分（占 60%），这是"宁可松匹配、不放过低税率"的策略

### 实际例子

| 原品名 | 原编码+税率 | 候选编码+税率 | 实际相似度 | 兜底后 | 最终得分 | 是否采纳 |
|--------|------------|-------------|-----------|--------|---------|---------|
| 沙滩用品 | 95062900(17.6%) | 95069900(0%) | 0.05 | **0.15** | 1.0×0.6+0.15×0.4=0.66 | ✓ 采纳 |
| 彩带 | 58063200(17.6%) | 58063900(0%) | 0.08 | **0.15** | 1.0×0.6+0.15×0.4=0.66 | ✓ 采纳 |

> 结论：Sheet2 看到 15% 时，实质含义是**该候选与你的品名几乎没有文字匹配，但因为同前缀且税率更低（Free），依然推荐了它。**

## 配置参数总览

| 参数 | 默认值 | 位置 | 用途 |
|------|--------|------|------|
| `min_similarity` | 0.15 | optimizer初始化 | 候选编码最低匹配度 |
| `exclude_anti_dumping` | false | optimizer初始化 | 排除反倾销标记商品 |
| `final_score_threshold` | 0.55 | 第200行 | 最低采纳阈值 |
| `tax_score_weight` | 0.60 | 第190行 | 最终得分中税率权重 |
| `similarity_weight` | 0.40 | 第190行 | 最终得分中匹配度权重 |
| `bigram_weight` | 0.50 | 第169行 | 相似度中bigram权重 |
| `char_weight` | 0.50 | 第169行 | 相似度中字符重叠权重 |

---

## 附录A：docs/ 样板文件的逻辑关系（文件格式层面）

```
example01                          example02/03/04
─────────                          ──────────────

装柜信息（68列）                     清单（17列）
04.03装柜信息(1).xlsx   ← 输入 →   xxx 清单.xlsx
  439 行数据                          45-91 行数据
  运单号码/提单/船名/航次/柜号...        单号/HS编码/材质/用途...

        │                                    │
        ▼                                    ▼
      解析器 parser.py                    解析器 parser.py
        │                                    │
        ▼                                    ▼
      映射器 mapper.py                    映射器 mapper.py
        │                                    │
        ▼                                    ▼
  ┌─────────────────────────────────────────────┐
  │        14 列标准清关格式 (统一输出)            │
  │  中文品名 | 英文品名 | 商品编码 | 材质 | 用途  │
  │  箱数 | 数量 | 单位 | 币制 | 单价 | 总价      │
  │  净重 | 毛重 | 原产国                         │
  └─────────────────────────────────────────────┘
        │
        ▼
  ┌─────────────────────────────────────────────┐
  │  优化器 optimizer.py                        │
  │  HS编码税率优化 → Sheet2 优化明细             │
  └─────────────────────────────────────────────┘
        │
        ▼
    输出清关文件
  ZIMUSNH21275749清关.xlsx       ← example01
  HDUJGLA26MC32436(1).xlsx       ← example02
  COSU6446868583清关资料.xlsx     ← example03
  SMLMNJPE5C277600清关资料.xlsx   ← example04
```

### 两种输入模板

| | 模板A：装柜信息 | 模板B：清单 |
|---|---|---|
| **列数** | 68 列 | 17 列 |
| **信息密度** | 含完整物流信息（船名/航次/柜号/FBA单号/收件地址...） | 纯商品申报信息 |
| **使用场景** | example01 | example02、03、04 |
| **数据来源** | 货代/仓库装柜明细 | 客户提供的商品清单 |

### 4 个样板的对应关系

| 样板 | 输入文件 | 类型 | 商品数 | 输出文件 | 验证点 |
|------|---------|------|--------|---------|--------|
| example01 | `04.03装柜信息(1).xlsx` | 装柜信息 | 439行 | `ZIMUSNH21275749清关.xlsx` | 68列复杂格式→14列标准格式 |
| example02 | `HDUJGLA26MC32436 清单.xlsx` | 清单 | 91行 | `HDUJGLA26MC32436(1).xlsx` | 大批量+发票装箱单模板 |
| example03 | `COSU6446868583 清单.xlsx` | 清单 | 45行 | `COSU6446868583清关资料.xlsx` | 中批量+清关资料模板 |
| example04 | `NJPE5C277600 清单.xlsx` | 清单 | 48行 | `SMLMNJPE5C277600清关资料.xlsx` | 中批量+清关资料模板 |

### 甲乙丙三方关系

- **甲方**：货主，提供原始装柜信息或商品清单
- **乙方**：清关行/报关行，人工将甲方数据转化为海关申报文件，并优化HS编码降税
- **丙方（我们）**：DutyWise系统，服务乙方，用程序替代人工操作

---

## 附录B：docs/ 样板的人工操作逻辑推理（大文件进→小文件出）

### 数据对比（修正版）

| | 案例一 | 案例二 | 案例三 | 案例四 |
|---|---|---|---|---|
| **来源** | 甲方直接发的 | 甲方发给乙方 | 甲方发给乙方 | 甲方发给乙方 |
| **大文件（输入）** | 装柜信息 439行 | 清单 91行 | 清单 45行 | 清单 48行 |
| **小文件（输出）** | 清关文件 ~10行 | 清关资料 10行 | 清关资料 11行 | 清关资料 13行 |
| **输入品类数** | — | 43个 | 29个 | 43个 |
| **输出品类数** | — | 10个 | 11个 | 13个 |
| **输入CTNS** | — | 1976 | 729 | 1523 |
| **输出CTNS** | — | 988(50%) | **729(100%✓)** | **1523(100%✓)** |
| **输入实重(kg)** | — | 31,478 | 10,431 | 13,539 |
| **输出毛重(kg)** | — | 15,739(50%) | **10,431(100%✓)** | **13,539(100%✓)** |
| **HS前缀交集** | — | **4个** | **4个** | **6个** |

> 修正：HS编码交集并非为零。example02-04 分别有4/4/6个前缀交集。
> example02 输出是50%因为文件用两个汇总行分了上/下半柜，第二汇总行的GW=31,478完全等于输入实重。

### 三大关键发现（修正版）

**① 重量和件数被忠实传递，是乙方的"锚点"**

example03/04 中，输出毛重=输入实重 精确到小数点一位，输出箱数=输入件数 完全一致。这些数据是海关查验和运费计算的基础，不能被篡改。

**② HS编码是选择性替换，约半数留在同章**

```
example03 输入23个前缀  输出8个  交集4个: {8471, 3926, 3924, 6303}
example04 输入25个前缀  输出10个  交集6个: {9017, 6702, 8302, 3924, 3926, 8306}
```

半数输出编码的HS章级（前4位）在输入中存在对应。乙方并非凭空造编码，而是：
- ~50%商品保留在原来HS章内（同章内归并+税率优化）
- ~50%商品跨到新的低税率章

**③ 品名归类+低申报是主要"调整"手段**

```
输入 (43个品名)                  输出 (10个品名)
邮件收纳架 / PU手袋 / 涤纶钱包     → 塑料贴纸 / 置物架 / PVC袋
智能日历机 / 一体机电脑             → 日历 / 键盘
化妆镜 / 茶具套装 / 蜻蜓饰品        → 花架 / 派对套装 / 扇子
```

输出总金额被压缩到输入的 2%~7%：

| | 输入金额 | 输出金额 | 比例 |
|---|---|---|---|
| example02 | 362,248 | 6,608 | 1.8% |
| example03 | 120,148 | 6,694 | 5.6% |
| example04 | 99,734 | 6,787 | 6.8% |

### 乙方人工操作流程（修正版）

```
甲方发来的货物清单（输入大文件）
         │
         ▼
  ┌─────────────────────────────────────────┐
  │           乙方人工操作流程                  │
  │                                         │
  │  ① 忠实传递：箱数(CTNS)、重量(实重→毛重)  │
  │     → 不修改，直接搬运（运费计税基础）        │
  │                                         │
  │  ② 品名重归类：把43个具体品名，           │
  │     归入10个低税率宽泛类别                │
  │                                         │
  │  ③ HS编码选择性替换：~50%留在同章，        │
  │     ~50%跨到低税率章                      │
  │                                         │
  │  ④ 低申报：压低申报单价，                 │
  │     大幅降低总金额以减少关税               │
  │                                         │
  │  ⑤ 填入模板：填入14列标准清关格式          │
  └─────────────────────────────────────────┘
         │
         ▼
    清关资料（输出小文件）
```

### 对DutyWise系统的启示

当前系统只实现了"格式化"（解析→映射→14列模板输出）和"保守优化"（同前缀内税率优化），但乙方的实际工作需要的是：

| 层 | 当前实现 | 乙方实际做法 | 差距 |
|---|---|---|---|
| **重量/件数传递** | ✓ 字段映射自然传递 | ✓ 忠实保留 | 已对齐 |
| **字段映射** | ✓ keyword匹配 | ✓ 手动填写 | 已对齐 |
| **14列模板** | ✓ generator.py | ✓ 人工填写 | 已对齐 |
| **HS优化** | 同4位前缀内优化 | **同章保留+跨章跳转** | 需扩展 |
| **品名归类** | 未实现 | **人工判断归类** | 缺少 |
| **金额处理** | 直接搬运 | **低申报策略** | 缺少 |

如果系统目标是"完整替代乙方人工"，那么HS编码优化的策略需要从"同前缀搜索"升级为"全量搜索+章节偏好"，品名归类需要引入归类规则，金额计算需要配置低申报策略。
